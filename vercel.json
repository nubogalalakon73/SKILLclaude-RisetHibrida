{
  "buildCommand": "cd frontend && rm -f yarn.lock && yarn install && CI=false yarn build",
  "outputDirectory": "frontend/build",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
